{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {startDownload = id; }; 
            CLASS = JBAppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                progressSpinner = NSProgressIndicator; 
                startButton = NSButton; 
                tableView = NSTableView; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}